flask>=2.3
pandas>=2.0
openpyxl>=3.1
werkzeug>=2.3
gunicorn>=21.2
ofxparse>=0.21
xlrd>=2.0
# PDF extraction — pdfplumber é suficiente para a maioria dos PDFs
pdfplumber>=0.11
# Fallbacks pesados (pymupdf, pytesseract, pdf2image) ficam em
# requirements-ocr.txt — exigem compilação C e binários do SO
# (Tesseract + Poppler). Instale só se precisar de PDF escaneado:
#     pip install -r requirements-ocr.txt

